meta {
  name: font formats list
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/font-formats?page={{page}}&limit={{limit}}
  body: none
  auth: inherit
}

params:query {
  page: {{page}}
  limit: {{limit}}
}
