meta {
  name: fonts list
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/fonts?lang={{lang}}&page={{page}}&limit={{limit}}
  body: none
  auth: inherit
}

params:query {
  lang: {{lang}}
  page: {{page}}
  limit: {{limit}}
}
