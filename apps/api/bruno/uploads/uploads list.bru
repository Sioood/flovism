meta {
  name: uploads list
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/uploads?page={{page}}&limit={{limit}}
  body: none
  auth: inherit
}

params:query {
  page: {{page}}
  limit: {{limit}}
}
