meta {
  name: projects list
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/projects?lang={{lang}}&page={{page}}&limit={{limit}}
  body: none
  auth: inherit
}

params:query {
  lang: {{lang}}
  page: {{page}}
  limit: {{limit}}
}
