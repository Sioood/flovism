meta {
  name: projects show
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/projects/{{projectId}}?lang={{lang}}
  body: none
  auth: inherit
}

params:query {
  lang: {{lang}}
}
