meta {
  name: auth login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/{{apiVersion}}/auth/api/login
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "email": "{{auth.defaultUserEmail}}",
    "password": "{{auth.defaultUserPassword}}"
  }
}
