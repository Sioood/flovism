meta {
  name: auth signup
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/signup
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "email": "{{auth.defaultUserEmail}}",
    "fullName": "{{auth.defaultUserFullName}}",
    "password": "{{auth.defaultUserPassword}}",
    "passwordConfirmation": "{{auth.defaultUserPassword}}"
  }
}
