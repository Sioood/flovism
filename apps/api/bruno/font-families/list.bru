meta {
  name: List font families
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/fonts/{{fontId}}/families?page={{page}}&limit={{limit}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  page: {{page}}
  limit: {{limit}}
}
